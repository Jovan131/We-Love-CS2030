High priority:
1. In slot-list and slots, change ID of ig to ig.name instead (in JSON output) (DONE!)
2. Add in users??: registration/login/logout capability


----------------------------------------------------------------------------------------------------------------

Low priority:
1. Make trailing slash ('/') in urls optional using regex
2. Make urls of ig-details case insensitive using regex
3. Host on PythonAnywhere first, then switch to Render when want to include Postgresql